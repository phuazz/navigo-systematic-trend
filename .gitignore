# Local normalised-dataset cache — regenerated by the pipeline every build.
# The committed, client-served copy lives in docs/data/.
/data/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Local tooling (machine-specific paths)
.claude/

# OS / editor
.DS_Store
Thumbs.db
*.swp
